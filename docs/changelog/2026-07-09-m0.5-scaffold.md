# 改动记录 · M0.5 工程脚手架

- **日期**:2026-07-09
- **里程碑/任务**:M0.5(T0.5.1–T0.5.5)
- **feature 分支**:`feat/m0.5-scaffold`
- **feature 提交**:`bda7ce4`(初版实现)→ `4990b4b`(审查整改)
- **合并 commit**:`2550c26`(--no-ff)

## 动机
Tally 一期实现的第一步(spec §11)。M0 数据可行性 spike 需真实凭证暂缓,先落地纯工程脚手架:repo 骨架、依赖/构建、pydantic 配置层、交易日历、MarketProfile、合成K线生成器与测试基线,为 M1 各模块提供底座。对应 spec §0/§2/§5.1.1/§10 与 §11 M0.5。

## 变更点
- **T0.5.1**:`pyproject.toml`(PEP 621,`requires-python>=3.11,<3.13`,uv 管理,3.12 venv;运行时/dev/dashboard/data 依赖分组;`numpy>=1.26,<2.5` 规避 2.5 stub 语法问题;`yfinance==0.2.54` pin);`.env.example`、`.gitignore`、`.pre-commit-config.yaml`(ruff/black/mypy);`src/tally/` 按 spec §2 建模块骨架;`[project.scripts] tally`。
- **T0.5.2**:`common/config/` 包(拆小文件)——pydantic v2 建模 §10 全部 yaml(market-keyed 嵌套);`deep_merge` 纯函数(null 删除 + 列表整体替换 + 不可变);`config/*.yaml` 全部真实文件(US.enabled=false)。
- **T0.5.3**:根 `CLAUDE.md`(三条铁律、目录约定、AC 门禁、`if market ==` 禁令、无魔法数字、.env 约定、fixture 录制回放)。
- **T0.5.4**:`common/calendar.py`——`is_trading_day/next/prev(day, market)` 按 market 分派(CN=SSE/US=NYSE,pandas_market_calendars 占位,CN 后续接 Tushare)。
- **T0.5.5**:`tests/synth/generators.py`——六形态合成K线 + 极端涨跌幅注入 + 涨跌停开关 + 事件日标注,固定种子可复现。
- **额外**:`common/market_profile.py`(§5.1.1 MarketProfile frozen dataclass);`cli.py`(typer 骨架)。

## AC 结果(逐条对照 spec §11 M0.5)
| AC | 结果 | 证据 |
|---|---|---|
| T0.5.1 pytest 空跑绿 + .env.example 存在 | ✅ | 135 passed;`.env.example` 已追踪 |
| T0.5.2 非法配置报错定位到字段 + 深合并/null 删除单测 | ✅ | `test_config.py`(缺字段/类型错/越界/typo 断言 `err["loc"]`;deep_merge 逐值比对) |
| T0.5.3 CLAUDE.md 内容与 spec 一致 | ✅ | 三条铁律原文 + 目录/门禁/fixture 说明齐全 |
| T0.5.4 2019–2026 is/next/prev + market 分派 | ✅ | `test_calendar.py`(春节/国庆/感恩节/圣诞真实日期 + 分派 + 越界 ValueError) |
| T0.5.5 六形态固定种子可复现 + 参数化 | ✅ | `test_generators.py`(`assert_frame_equal` 逐值 + 极端/涨跌停/事件日 + 小 n_days 钳制) |

## 代码审查结论(2 位资深 Agent)
- **code-reviewer(通用/铁律)**:WARNING → 整改后无 CRITICAL/HIGH。发现 H3(AST 禁令可绕过)、M(配额和无校验)等,已修。
- **python-reviewer(Python/类型)**:Block → 整改后 Approve。发现 H1(black)、H2(工具链版本)、M(.env 参数/secret min_length/合成K线崩溃),已修。

## 测试结论(2 位测试 Agent)
- **tdd-guide(覆盖率/质量)**:放行。行覆盖 97%→99%,断言逐值非假绿;补齐 AST 反向测试、MarketProfile/.env/config 错误路径等缺口。
- **测试工程师(独立 AC/边界)**:五条 AC 独立复现通过、完全离线、config 与 §10 零偏差、防未来函数接口就绪。

## 整改(审查后,commit `4990b4b`)
- HIGH×3:black 重排;工具链 target 对齐 py311/3.11(连带 pin numpy<2.5);AST 禁令补 `in`/`not in`/`match-case` 检测 + 8 组违规反向测试 + 3 组合法负向对照。
- MEDIUM×4:.env 路径去重(替换布尔锁);secret 字段加 min_length=1;`QuotasConfig` 加 s1+s2+s4≤1 校验;合成K线 crash/recover 小 n_days 钳制。
- 补测:MarketProfile 逐字段、portfolio/pool/backtest 配置错误路径、日历越界。LOW:.env.example 去 SEC_USER_AGENT 误导、依赖注释、deep_merge docstring、裸 dict→dict[str,Any]、mypy hook 范围。

## main 合并前门禁(main 亲自复核)
```
uv run pytest -q                 → 135 passed
uv run pytest --cov=src/tally    → 99% cover (378 stmts, 1 miss)
uv run ruff check .              → All checks passed
uv run black --check .           → 31 files unchanged
uv run mypy src tests            → Success: no issues in 31 files
```
AST 禁令独立探针:8 种违规写法全检出、3 种合法写法不误报。

## 回归结论(合并后)
- 测试 Agent:**通过**。main 上 135 passed;连跑 4 次(默认/no-randomly/no-cacheprovider)结果一致无 flaky;阻断网络+清空凭证仍 135 passed(离线可跑);ruff/black/mypy 全绿;46 文件完整无冲突残留;`tally --help/--version` 正常。提示:复现须用 `uv sync --extra dev`(dev 工具在 optional-deps,裸 `uv sync` 会卸载),建议后续迁 PEP 735。
- 产品经理 Agent:**通过(有条件)**。M0.5 五任务逐条达标,US.enabled=false、通知=Telegram、config 与 §10 零偏差三决策全部落实,M1 底座就位无阻塞;条件仅为补齐本合并 commit 与回归结论行,及将"缺失密钥 loud failure"并入 M1 T1.2、"CN 日历接 Tushare"排入 M1 早期跟踪。

## 风险与后续
- CN 交易日历目前用 pandas_market_calendars(SSE)占位,M1/M2 接入 Tushare trade_cal 落库后需对齐(spec T0.5.4/T2 口径)。
- 缺失密钥的"loud failure"尚未落地,需在 M1 数据源适配层实现(现仅返回空串 + min_length 校验)。
- AST 禁令为尽力而为 lint,别名/getattr/跨函数写法需人工 code review 兜底(已在 docstring 标注)。
- numpy 上限 `<2.5` 为规避 stub 语法问题,后续 numpy 修复门控后可放开。
