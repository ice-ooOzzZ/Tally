# 改动记录 · M1 T1.1 最小 Repository + WAL 单写线程

- **日期**:2026-07-09
- **里程碑/任务**:M1 / T1.1
- **feature 分支**:`feat/m1-repository`
- **feature 提交**:`7b9ac65`(实现)→ `8d91233`(审查整改)
- **合并 commit**:`0d0b35f`(--no-ff)

## 动机
M1 垂直切片的地基:所有持久化的唯一入口(spec §0 铁律 3)。提供 kline/valuation/signals 三表的读写、WAL + 单写线程的并发模型,为 T1.2 起的同步/策略/CLI 提供落库能力。对应 spec §3.2(三表 DDL)、§3.3(单写线程)。

## 变更点
- `src/tally/data/schema.sql`:kline(含 source 列)/valuation/signals 三表 + 索引 idx_kline_md、idx_signals_date(仅本任务三表;全量 DDL 是 M2 T2.1)。补 NOT NULL / IF NOT EXISTS,已在文件头注明为非语义变更。
- `src/tally/data/repository.py`:`Repository` 门面——只读连接(mode=ro URI,路径 quote+resolve)做读;写经 WriteQueue;get/upsert/insert 方法带 market 维度;上下文管理器 + 幂等 close。
- `src/tally/data/_writer.py`:单写线程 + `queue.Queue`,writer 异常时 fail-all 防死锁;WAL PRAGMA 回读校验(非 wal 拒绝启动);close 双标志(已投递 _STOP / 已确认退出);atexit 安全网。
- `src/tally/data/_rows.py`:行归一化,`pd.isna` 必填校验 + 默认值回退。
- 设计:裸 sqlite3 无 ORM;PRAGMA WAL/busy_timeout=30000/synchronous=NORMAL/foreign_keys=ON;ON CONFLICT upsert(非 INSERT OR REPLACE);SQL 只在 data/ 内。

## AC 结果(对照 spec §11 T1.1)
| AC | 结果 | 证据 |
|---|---|---|
| 并发读写压测无 lock 错误 | ✅ | `test_repository_concurrency.py`(8写+8读×20共享PK×parametrize(3));独立压测 40 线程/15s/6880 写 0 lock;探针证明压测非假绿(未修版能压出 16 次 locked) |

## 代码审查结论(2 位资深 Agent)
- **code-reviewer(通用/并发/铁律)**:WARNING → 整改后清零。发现 HIGH(close 幂等假成功,已复现);铁律3/方言/PRAGMA/market 收口/schema 一致性均通过。
- **python-reviewer(Python/类型/线程)**:通过(Warning)→ 整改后清零。发现 MEDIUM(submit Any / daemon 静默丢任务);确认自查的 3 个历史 HIGH 已修且有回归测试。

## 测试结论(2 位测试 Agent)
- **tdd-guide(覆盖率/质量)**:放行。src/tally/data 覆盖率 99%;用"未修版"探针证明并发压测非假绿;列 7 项非阻塞补测,已补高价值 4 项。
- **测试工程师(独立 AC/压测)**:放行。自写高强度压测(40 线程/6880 写)0 lock、无损坏、writer 存活;边界/死锁防护/schema/WAL/离线全过。

## 整改(审查后,commit `8d91233`)
- HIGH:close() 拆"已投递 _STOP / 已确认退出"双标志,每次 close 都重新 join+检查 is_alive,存活即 raise(补回归测试)。
- MEDIUM:submit 加 TypeVar 泛型(mypy --strict 无 no-any-return);daemon writer 加 atexit warning 安全网(含子进程端到端测试)。
- LOW:_fail_all_pending 异常链 `__cause__=exc` 对齐。补测:并发压测纳入 upsert_valuation、单边范围查询、空结果 DataFrame 列名、valuation NaN→None 对称。

## main 合并前门禁(main 亲自复核)
```
uv run pytest -q                    → 194 passed
--cov=src/tally/data                → 99%(仅 2 行防御分支未覆盖)
uv run ruff check .                 → All checks passed
uv run black --check .              → 39 files unchanged
uv run mypy src tests               → Success
uv run mypy --strict src/tally/data → Success
```
HIGH(close 幂等)回归测试独立复核通过。

## 回归结论(合并后)
- 测试 Agent:**通过**。main 上 194 passed;连跑 2 次结果一致无 flaky;清凭证+死代理阻断网络后 tests/data 59 passed(离线可跑);ruff/black/mypy/mypy --strict 全绿;合并 10 文件/+1693 行完整无冲突标记;M0.5 的 135 用例无回归。
- 产品经理 Agent:**通过**。硬 AC「并发读写压测无 lock」独立高强度复现达成(40线程/6880写 0 lock),三表读写与 upsert_kline/upsert_valuation/insert_signal 接口就位带 market 维度,铁律3(SQL 仅 data/)、裸 sqlite3 无 ORM、WAL 单写线程、ON CONFLICT 方言纪律全合规,T1.2 可无阻塞启动;3 条遗留风险(单线程写吞吐/2行防御分支/flock 属 T4.3)均可接受。

## 风险与后续
- writer 单线程串行写,写吞吐有上限(符合"多线程拉取单线程批量写"规格);M2 写量大时可关注批量合并。
- 剩 2 行防御分支(lastrowid None / _fail_all_pending 跳过 _STOP)命中成本高,未强求覆盖。
- 跨进程写串行化(flock)是 T4.3 的事,本任务单进程内单写线程已足够。
