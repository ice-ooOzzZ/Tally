# 改动记录 · M1 T1.2 Tushare 适配器 + 令牌桶 + fixture 回放

- **日期**:2026-07-09
- **里程碑/任务**:M1 / T1.2
- **feature 分支**:`feat/m1-tushare-source`
- **feature 提交**:`0a88c33`(实现)→ `b25f51b`(审查整改)
- **合并 commit**:`<合并后填>`
- **流水线档位**:精简档(1 开发 + 1 合并审查 + 1 测试 + 1 回归)

## 动机
M1 数据入口:把 A股行情/复权/估值从 Tushare 拉进来并归一化,供 T1.3 sync 层落库。同时落地全局令牌桶(§3.3)、fixture 录制回放(离线可测)、缺失密钥 loud failure(PM 建议在首个触网任务落地)。对应 spec §3.1(#1-3 daily/adj_factor/daily_basic)、§3.3、§0。

## 变更点
- `src/tally/data/rate_limit.py`:`TokenBucket`(速率型,线程安全,时钟/sleep 可注入)+ `RateLimiterRegistry`(按 source 隔离,get_or_create 双检锁)。
- `src/tally/data/sources/tushare_source.py`:`TushareSource` 适配器——`fetch_daily/fetch_adj_factor/fetch_daily_basic/fetch_kline`,归一化列对齐 schema(YYYYMMDD→YYYY-MM-DD、代码去后缀、amount 千元→元、total_mv 万元→元、跨除权日 adj_factor 左连接);`TushareTransport` Protocol 依赖注入;经令牌桶 + tenacity 重试;缺 token/空白 token/未装 tushare 均 loud failure(调用时,非 import 时)。
- `tests/data/tushare_fixtures.py` + `tests/fixtures/tushare/*.json` + README:ReplayTransport(离线回放)+ RecordingTransport(待真 token 录制替换);手工构造覆盖三接口 + 跨除权日样本。
- 适配器**不碰 Repository、不写 SQL**(落库留 T1.3)。

## AC 结果(对照 spec §11 T1.2)
| AC | 结果 | 证据 |
|---|---|---|
| 回放单测通过、CI 不打真实 API | ✅ | `env -u TUSHARE_TOKEN` 下 242 passed;测试零 `import tushare`、无网络库;目标文件 100% 覆盖;ReplayTransport 走本地 JSON |

## 审查结论(1 合并审查:代码质量+Python+安全一体)
放行(Approve),无 CRITICAL/HIGH。发现 3 MEDIUM(codes=[] 全市场拉取、惰性建连无锁、异常重试范围宽)+ 1 LOW(registry 无锁);亮点:令牌桶锁粒度正确、loud failure 分层、依赖注入干净不 import tushare、铁律合规、无硬编码密钥、fixture 无敏感数据。

## 测试结论(1 合并测试:覆盖+AC+边界一体)
放行。AC 完全达成,目标文件 100% 覆盖,独立证实完全离线,归一化/单位换算/adj_factor 合并/令牌桶限流数学/重试均真断言;fixture 诚实。4 项 LOW 补测缺口已在整改补齐。

## 整改(审查后,commit `b25f51b`)
- F1:`codes=[]` 短路返回空 DataFrame、不发起调用(区分 None=全市场);F2:惰性建连双检锁;F3:RateLimiterRegistry.get_or_create 双检锁;F4:token `.strip()` 空白按缺失 loud failure。
- 补测:多 codes 令牌消耗、缺关键列 KeyError、merge adj 多出日期丢弃方向。
- 不改:转输层异常统一包装重试范围偏宽(已知偏离,异常链保原始错,文档已说明)。

## main 合并前门禁(main 亲自复核,均 `env -u TUSHARE_TOKEN` 离线)
```
uv run pytest -q               → 242 passed
--cov=src/tally/data           → rate_limit.py / tushare_source.py 均 100%
uv run ruff check .            → All checks passed
uv run black --check .         → 44 files unchanged
uv run mypy src tests          → Success
关键修复回归(codes=[]短路/并发建连/registry/token strip)→ 10 passed
```

## 回归结论(合并后)
- 测试 Agent:`<合并后填>`
- 产品经理 Agent:`<合并后填>`

## 风险与后续
- `turnover_amt` 恒 NaN(daily_basic 不提供成交额;T1.3 sync 层从 daily.amount 关联填充)。
- fixture 为手工构造,待有真实 TUSHARE_TOKEN 时用 RecordingTransport 录制替换(README 有流程)。
- 转输层异常统一当瞬时重试:真编程错误会重试 3 次才暴露(异常链可追溯);后续可白名单收紧。
