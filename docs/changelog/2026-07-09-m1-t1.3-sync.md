# 改动记录 · M1 T1.3 最小同步管道(增量 + as_of_date + 手工名单)

- **日期**:2026-07-09
- **里程碑/任务**:M1 / T1.3
- **feature 分支**:`feat/m1-sync`
- **feature 提交**:`240e40a`(实现)→ `9dd70e7`(审查整改)
- **合并 commit**:`<合并后填>`
- **流水线档位**:精简档(1 开发 + 1 合并审查 + 1 测试 + 1 回归)

## 动机
把 T1.2 的 Tushare 适配器接到 T1.1 的 Repository:手工名单(config 20 只票)按缺失区间增量拉日K+估值落库,as_of_date 贯穿(防未来函数)。对应 spec §11 T1.3、§3.1(#1-3)、§3.3、§0 铁律1。

## 变更点
- `config/watchlist.yaml` + `common/config/watchlist.py`(WatchlistConfig:market/start_date/codes,StrictModel/禁重复/min_length)。
- `common/calendar.py`:新增 `trading_days_in_range(start, end, market)`(start>end 返回空)。
- `data/sync.py`:`SyncEngine.sync(market, as_of_date, transport, repo)`——增量(查各表最新日期算缺失区间)、落库前 `_clip_to_as_of` 二次截断(防未来函数真防线,不依赖 transport 守 end_date)、turnover_amt 从 daily.amount 关联填充、令牌桶经 RateLimiterRegistry 复用、transport 依赖注入(离线可测)、单票失败不阻塞其他票。
- `data/repository.py`:新增 `get_latest_kline_date`/`get_latest_valuation_date`(ORDER BY date DESC LIMIT 1,避免全表扫描)。

## AC 结果(对照 spec §11 T1.3)
| AC | 结果 | 证据 |
|---|---|---|
| 跨除权日增量更新后复权收益序列连续(模拟除权事件) | ✅ | `test_incremental_sync_across_ex_dividend_event...`:除权落在第二次增量批次,边界收益率与手工重算 `(10.20×1.25)/(10.50×1.20)−1` 一致、无 NaN |
| 增量按缺失区间(§3.3) | ✅ | 二次 sync 只请求缺失日期;同 as_of no-op(_BoomTransport 零调用) |
| 防未来函数(铁律1) | ✅ | 泄漏 transport(忽略 end_date)+ 2099 远期行 双重施压,均被 `_clip_to_as_of` 截断,kline+valuation 双表不落超前数据 |

## 审查结论(1 合并审查:代码+Python+铁律一体)
Block → 整改后清零。发现 1 HIGH(kline/valuation 非原子写入 → 单票部分失败致永久 valuation 缺口 + CodeSyncResult 行数失真,已复现);铁律1 独立核查**无漏洞**、复权连续性正确、铁律3 合规、config/类型/lint 达标。4 MEDIUM + LOW 一并修。

## 测试结论(1 合并测试:覆盖+AC+防未来函数一体)
放行。sync.py 100% 覆盖、离线、无 flaky;跨除权日/增量/防未来函数均真断言 + 独立黑盒复现(含 2099 远期行施压)。

## 整改(审查后,commit `9dd70e7`)
- HIGH:采用**自愈式续传起点**——`_resolve_missing_start` 取 min(kline最新, valuation最新),任一表缺口下次 sync 必被补齐;新增 `get_latest_*_date` 避免全表扫描;修正 CodeSyncResult 异常路径行数如实反映(kline_rows=实际写入)。自愈证据:valuation 写入失败→下次 sync 补齐,最终两表行数一致。
- MEDIUM:trading_days_in_range 直接单测;续传不再拉全历史(测试证明不调 get_kline);_fill_turnover_amt 落库前重复键 (code,market,date) 断言去重;as_of 早于数据的 no-op 分支加注释。
- 补测:as_of 超前数据源可得日 → 反复重拉尾部缺口(文档化设计内行为)。LOW:Counter 简化、reset_index。

## main 合并前门禁(main 亲自复核,均离线 `env -u TUSHARE_TOKEN`)
```
uv run pytest -q            → 273 passed
--cov=src/tally/data        → sync.py 100%(全模块 99%,仅 2 行历史遗留防御分支)
uv run ruff check .         → All checks passed
uv run black --check .      → 48 files unchanged
uv run mypy src tests       → Success
关键修复回归(自愈/防重复/日历单测/超前数据)→ 9 passed
```

## 回归结论(合并后)
- 测试 Agent:`<合并后填>`
- 产品经理 Agent:`<合并后填>`

## 风险与后续
- `sync_failures` 表持久化 + 连续 3 天失败 Telegram 告警(§3.3)未实现——本任务只做到"单次运行内失败不阻塞、记入 CodeSyncResult.error";留后续任务补失败重试队列落库。
- 顶层装配入口(CLI `tally sync` 读 system.yaml/watchlist.yaml 构造 SyncEngine)未接——本任务只交付 SyncEngine 本体 + config 模型;装配留 T1.6/后续。
- 串行执行(未用 ThreadPoolExecutor);_sync_one_code 已设计为无共享可写状态、可安全并发,20 票规模未引入并发。
- as_of 超前数据源可得日会反复重拉尾部缺口(设计内,已文档化测试)。
- Tushare 若修订历史 adj_factor,增量不回刷旧日期(增量爬虫通用限制,非本任务缺陷)。
